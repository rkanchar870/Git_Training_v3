pipeline {
      agent any
      stages {
           stage('Init') {
	        steps {
		    echo "Hello everyone, this is rajesh kancharla"
		    }
		    }
           stage('Build') {
	         steps {
		    echo "This is build stage"
		    }
		    }
           stage('Deploy') {
                     steps {
		         echo "This is Deploy stage"
			 }
       }
    }
    }
